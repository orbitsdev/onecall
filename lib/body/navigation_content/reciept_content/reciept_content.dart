import 'package:flutter/material.dart';

class RecieptContent extends StatelessWidget {
const RecieptContent({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return  Container(
      child: Center(child: Text('Reciept'),),
    );
  }
}